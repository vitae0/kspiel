"use client";import {useEffect,useRef} from "react";import maplibregl from "maplibre-gl";export default function Home(){const el=useRef<HTMLDivElement>(null);useEffect(()=>{if(!el.current)return;const map=new maplibregl.Map({container:el.current,style:"https://demotiles.maplibre.org/style.json",center:[29,41],zoom:5});map.addControl(new maplibregl.NavigationControl({showCompass:false}),"bottom-right");return()=>map.remove()},[]);return <main><div ref={el} className="map"/><header><b>KSPIEL</b><span>PAUSED</span><button>▶</button></header><nav><button>☰</button><button>FORCES</button><button>SUPPLY</button><button>POLITICS</button><button>R&D</button></nav><aside><b>No formation selected</b><small>Tap a unit on the map.</small></aside></main>}